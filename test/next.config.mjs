/** @type {import('next').NextConfig} */
const nextConfig = {images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'unsvadtyxgvyjhydsboc.supabase.co',
        port: '',
        
      },
    ],
  },};

export default nextConfig;
